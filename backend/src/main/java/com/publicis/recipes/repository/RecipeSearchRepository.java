package com.publicis.recipes.repository;

/*
 * Hibernate Search Repository - Temporarily disabled for compatibility
 * This will be re-enabled once we resolve the Jakarta EE vs Java EE compatibility issues
 */

/*
import com.publicis.recipes.entity.Recipe;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import org.hibernate.search.engine.search.query.SearchResult;
import org.hibernate.search.mapper.orm.Search;
import org.hibernate.search.mapper.orm.session.SearchSession;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.PageImpl;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Repository;

import java.util.List;
*/

/**
 * Repository for full-text search operations using Hibernate Search.
 *
 * This repository provides advanced search capabilities using Lucene indexing
 * for efficient full-text search across recipe names and cuisines.
 *
 * @author Recipe Management Team
 * @version 1.0.0
 */
// @Repository - Temporarily disabled
public class RecipeSearchRepository {
    // Temporarily disabled - will be re-enabled with compatible Hibernate Search version
}